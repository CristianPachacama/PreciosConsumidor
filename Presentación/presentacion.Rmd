---
title: Análisis Clúster IPC
subtitle: Instituto de Investigaciones Económicas
author: Cristian Pachacama
date: Mayo, 2018
widgets: [bootstrap, quiz]
hitheme     : tomorrow      # 
mode        : selfcontained # {standalone, draft}
output:
  ioslides_presentation:
    theme: cerulean
    logo: images/logo_icon.png
    smaller: yes
    transition: slower
    widescreen: yes
    slidy_presentation: default
---

```{r,echo=FALSE, message=FALSE, warning=FALSE}
library(knitr)
library(DT)
library(readr)
library(forecast)
library(TSdist)
library(TSclust)
library(smacof)
library(dygraphs)

load("Data/DatosAux.RData")

knitr::opts_chunk$set(echo = FALSE,message=FALSE, warning=FALSE)
#opts_chunk$set(echo = TRUE, eval = FALSE, message=FALSE, warning=FALSE)
```

# Análisis Clúster

## Análisis del IPC

Para el anális se considera el Índice de Precios al Consumidor de todos los bienes de la canasta básica familiar (series mensuales de los últimos 13 años).Podemos encontrar las series de los siguientes Productos:

```{r}
BDD=data.frame(Producto=Items,Abrebiatura=items)
datatable(BDD)
```


## Cluster 1

Se procedió a realizar el análisis Clúster de las series asociadas a estos productos, para el análisis se consideró la métrica de la "Autocorrelación" que comprar la correlación temporal de las series. Se obtuvieron los siguientes resultados.

```{r}
graf_series(IPC,items,cl[[1]])
```


## Cluster 2

```{r}
graf_series(IPC,items,cl[[2]])
```

## Cluster 3

```{r}
graf_series(IPC,items,cl[[3]])
```


## Cluster 4

```{r}
graf_series(IPC,items,cl[[4]])
```

## Cluster 5

```{r}
graf_series(IPC,items,cl[[5]])
```


## Cluster 6

```{r}
graf_series(IPC,items,cl[[6]])
```



